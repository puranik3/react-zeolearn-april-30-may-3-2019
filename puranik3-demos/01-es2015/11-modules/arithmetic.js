const sum = ( x, y ) => x + y;
const diff = ( x, y ) => x - y;
const mult = ( x, y ) => x - y;

export {
    sum,
    mult
};

// one default export per module
export default {
    PI: 3.14
};

// you can define a class/function/object etc. and export 