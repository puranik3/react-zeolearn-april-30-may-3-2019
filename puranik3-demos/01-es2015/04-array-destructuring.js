const days = [
    'Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday'
];

// const first = days[0], second = days[1], third = days[2];
const [ first, second, third, , fifth ] = days;
console.log( first, second, third, fifth );